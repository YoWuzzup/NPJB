import { FC } from "react";

import { Input } from "@/components/common/Input";

export const WriteReview: FC = () => {
  return (
    <div className="w-full h-full">
      <h3 className="text-lg font-bold my-3 capitalize">your new review</h3>
      <Input inputProps={{}} />
    </div>
  );
};
